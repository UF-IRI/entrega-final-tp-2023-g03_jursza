#ifndef CLASE_H
#define CLASE_H

#endif // CLASE_H

#include "spinning.h"
#include "cliente.h"

struct clase{
    struct Clase {
        sSpinning spinning;
        // sYoga yoga;

    }; typedef struct Clase sClase;
};

void reservarTurno(sClase* clases, sCliente cliente);
